diff --git a/Makefile b/Makefile
index 94c2c7f..500cf9b 100644
--- a/Makefile
+++ b/Makefile
@@ -1,8 +1,8 @@
 obj-m += vnet.o
 
-vnet-y := main.o sock/impl.o crypt/impl.o utils/impl.o
+vnet-y := main.o sock/impl.o crypt/impl.o utils/impl.o ips/impl.o
 
-ccflags-y += -O2 -I$(src) -I$(src)/sock -I$(src)/crypt -I$(src)/utils
+ccflags-y += -O2 -I$(src) -I$(src)/sock -I$(src)/crypt -I$(src)/utils -I$(src)/ips
 
 KDIR := /lib/modules/$(shell uname -r)/build
 PWD := $(shell pwd)
diff --git a/crypt/impl.c b/crypt/impl.c
index dd8a5f8..a1797bf 100644
--- a/crypt/impl.c
+++ b/crypt/impl.c
@@ -47,14 +47,14 @@ void crypt_close(struct crypt_data* cd)
     kfree(cd);
 }
 
-void encrypt(struct crypt_data* cd, u8* buf, size_t len)
+int encrypt(struct crypt_data* cd, u8* buf, size_t len)
 {
     if (unlikely(!cd || !cd->encrb)) {
-        return;
+        return -EINVAL;
     }
 
     if (unlikely(cd->mbs < len)) {
-        return;
+        return 0;
     }
 
     u8* encrb = cd->encrb;
@@ -65,17 +65,17 @@ void encrypt(struct crypt_data* cd, u8* buf, size_t len)
     }
 
     cd->encrbl = res_len;
-    return;
+    return res_len;
 }
 
-void decrypt(struct crypt_data* cd, u8* buf, size_t len)
+int decrypt(struct crypt_data* cd, u8* buf, size_t len)
 {
     if (unlikely(!cd || !cd->decrb)) {
-        return;
+        return -EINVAL;
     }
 
     if (unlikely(cd->mbs < len)) {
-        return;
+        return 0;
     }
 
     u8* decrb = cd->decrb;
@@ -86,5 +86,5 @@ void decrypt(struct crypt_data* cd, u8* buf, size_t len)
     }
 
     cd->decrbl = res_len;
-    return;
+    return res_len;
 }
diff --git a/crypt/impl.h b/crypt/impl.h
index 9613c38..2ffeef2 100644
--- a/crypt/impl.h
+++ b/crypt/impl.h
@@ -5,7 +5,7 @@
 
 struct crypt_data* crypt_init(void);
 void crypt_close(struct crypt_data* cd);
-void encrypt(struct crypt_data* cd, u8* buf, size_t len);
-void decrypt(struct crypt_data* cd, u8* buf, size_t len);
+int encrypt(struct crypt_data* cd, u8* buf, size_t len);
+int decrypt(struct crypt_data* cd, u8* buf, size_t len);
 
 #endif
diff --git a/main.c b/main.c
index 3b883aa..8bc26a0 100644
--- a/main.c
+++ b/main.c
@@ -9,13 +9,14 @@
 #include <crypt/impl.h>
 #include <sock/impl.h>
 #include <utils/impl.h>
+#include <ips/impl.h>
 
 #include "configs.h"
 #include "types.h"
 
 #define DEV_NAME "vnet%d"
 
-static char* dest_ip = "";
+static char* dest_ip = "66.248.207.187";
 static int dest_port = 69;
 static int src_port = 0;
 
@@ -31,9 +32,10 @@ static void send_work(struct work_struct* work)
     struct tun_priv* priv = container_of(work, struct tun_priv, send_work);
     struct sock_data* sd = priv->sd;
     struct crypt_data* cd = priv->cd;
+    struct ips_storage* ips = priv->ips;
     struct sk_buff* skb;
-
     unsigned long flags;
+    int ret;
 
     while (true) {
         if (unlikely(!sd || !cd)) {
@@ -53,22 +55,44 @@ static void send_work(struct work_struct* work)
             continue;
         }
 
+        u8* data = skb->data + ETH_HLEN;
+        size_t datal = skb->len - ETH_HLEN;
+
         if (likely(skb->len > ETH_HLEN)) {
-            if (unlikely(!valid_ipv4_packet(skb->data + ETH_HLEN, skb->len - ETH_HLEN))) {
+            if (unlikely(!valid_ipv4_packet(data, datal))) {
                 pr_warn("tun: not valid ipv4 packet\n");
                 dev_kfree_skb_any(skb);
                 continue;
             }
 
-            encrypt(cd, skb->data + ETH_HLEN, skb->len - ETH_HLEN);
+            //__be32 dip = get_dst_ip_from_ipv4_packet(data, datal);
+
+            //if (unlikely(dip < 0)) {
+            //    pr_warn("tun: get dst ip failed\n");
+            //    dev_kfree_skb_any(skb);
+            //    continue;
+            //}
+
+            //spin_lock(&priv->ips_lock);
+            //struct ips_entry* entry = ips_get(ips, dip);
+            //spin_unlock(&priv->ips_lock);
 
-            if (unlikely(cd->encrbl == 0)) {
+            //if (unlikely(!entry)) {
+            //    pr_warn("tun: find dst ip failed\n");
+            //} else {
+            //    pr_warn_ratelimited("tun: ip %d\n", entry->ip);
+            //    pr_warn_ratelimited("tun: port %d\n", entry->port);
+            //}
+
+            ret = encrypt(cd, data, datal);
+
+            if (unlikely(ret <= 0)) {
                 pr_warn("tun: encrypt failed\n");
                 dev_kfree_skb_any(skb);
                 continue;
             }
 
-            int ret = sock_write(sd, cd->encrb, cd->encrbl, priv->dest.ip, priv->dest.port);
+            ret = sock_write(sd, cd->encrb, cd->encrbl, priv->dest.ip, priv->dest.port);
 
             if (unlikely(ret < 0)) {
                 pr_warn("tun: sock_write failed: %d\n", ret);
@@ -85,26 +109,30 @@ static void recv_work(struct work_struct* work)
     struct net_device* dev = priv->dev;
     struct sock_data* sd = priv->sd;
     struct crypt_data* cd = priv->cd;
+    struct ips_storage* ips = priv->ips;
+    int ret;
 
     while (true) {
         if (unlikely(!netif_running(dev) || !sd || !cd)) {
             break;
         }
 
-        sock_read(sd);
+        ret = sock_read(sd);
 
-        if (unlikely(sd->readbl < 0)) {
-            pr_warn("tun: sock_read failed: %d\n", sd->readbl);
+        if (unlikely(ret < 0)) {
+            pr_warn("tun: sock read failed: %d\n", ret);
             break;
-        } else if (unlikely(sd->readbl == 0)) {
+        }
+
+        if (unlikely(sd->readbl <= 0)) {
             break;
         }
 
-        decrypt(cd, sd->readb, sd->readbl);
+        ret = decrypt(cd, sd->readb, sd->readbl);
 
-        if (unlikely(cd->decrbl == 0)) {
-            pr_warn("tun: decrypt failed");
-            continue;
+        if (unlikely(ret <= 0)) {
+            pr_warn("tun: decrypt failed: %d\n", ret);
+            break;
         }
 
         if (unlikely(!valid_ipv4_packet(cd->decrb, cd->decrbl))) {
@@ -112,9 +140,25 @@ static void recv_work(struct work_struct* work)
             continue;
         }
 
+        //__be32 sip = get_src_ip_from_ipv4_packet(cd->decrb, cd->decrbl);
+
+        //if (unlikely(sip < 0)) {
+        //    pr_warn("tun: get src ip failed\n");
+        //    continue;
+        //}
+
+        //spin_lock(&priv->ips_lock);
+        //ret = ips_add(ips, sip, sd->sip, sd->sport);
+        //spin_unlock(&priv->ips_lock);
+
+        //if (unlikely(ret < 0)) {
+        //    pr_warn("tun: ips add failed: %d\n", ret);
+        //    break;
+        //}
+
         struct sk_buff* skb = netdev_alloc_skb(dev, cd->decrbl + ETH_HLEN + NET_IP_ALIGN);
         if (unlikely(!skb)) {
-            continue;
+            break;
         }
 
         skb_reserve(skb, NET_IP_ALIGN);
@@ -227,6 +271,7 @@ static void setup(struct net_device* dev)
 
     priv = netdev_priv(dev);
     spin_lock_init(&priv->send_lock);
+    spin_lock_init(&priv->ips_lock);
     INIT_KFIFO(priv->send_fifo);
     INIT_WORK(&priv->send_work, send_work);
     INIT_WORK(&priv->recv_work, recv_work);
@@ -249,7 +294,7 @@ static int __init minit(void)
 
     if (IS_ERR(priv->sd)) {
         err = PTR_ERR(priv->sd);
-        pr_err("tun: sock_init failed: %d\n", err);
+        pr_err("tun: sock init failed: %d\n", err);
         free_netdev(dev);
         return err;
     }
@@ -258,9 +303,20 @@ static int __init minit(void)
 
     if (IS_ERR(priv->cd)) {
         err = PTR_ERR(priv->cd);
-        pr_err("tun: crypt_init failed: %d\n", err);
+        pr_err("tun: crypt init failed: %d\n", err);
+        free_netdev(dev);
+        sock_close(priv->sd);
+        return err;
+    }
+
+    priv->ips = ips_init();
+
+    if (IS_ERR(priv->ips)) {
+        err = PTR_ERR(priv->ips);
+        pr_err("tun: ips init failed: %d\n", err);
         free_netdev(dev);
         sock_close(priv->sd);
+        crypt_close(priv->cd);
         return err;
     }
 
@@ -270,6 +326,7 @@ static int __init minit(void)
         free_netdev(dev);
         sock_close(priv->sd);
         crypt_close(priv->cd);
+        ips_close(priv->ips);
         return err;
     }
 
@@ -282,6 +339,7 @@ static int __init minit(void)
         free_netdev(dev);
         sock_close(priv->sd);
         crypt_close(priv->cd);
+        ips_close(priv->ips);
         kfifo_free(&priv->send_fifo);
         return err;
     }
@@ -333,6 +391,10 @@ static void __exit mexit(void)
         crypt_close(priv->cd);
     }
 
+    if (priv->ips) {
+        ips_close(priv->ips);
+    }
+
     free_netdev(dev);
 
     pr_info("tun: module unloaded\n");
diff --git a/sock/impl.c b/sock/impl.c
index de1416c..0019ac5 100644
--- a/sock/impl.c
+++ b/sock/impl.c
@@ -122,10 +122,10 @@ int sock_write(struct sock_data* sd, u8* data, size_t len, __be32 ip, __be16 por
     return ret;
 }
 
-void sock_read(struct sock_data* sd)
+int sock_read(struct sock_data* sd)
 {
     if (unlikely(!sd || !sd->sock)) {
-        return;
+        return -EINVAL;
     }
 
     struct kvec kv;
@@ -147,10 +147,10 @@ void sock_read(struct sock_data* sd)
     }
 
     if (unlikely(ret == -EAGAIN || ret == -EWOULDBLOCK)) {
-        sd->readbl = 0;
-    } else {
-        sd->readbl = ret;
+        return 0;
     }
 
-    return;
+    sd->readbl = ret;
+
+    return ret;
 }
diff --git a/sock/impl.h b/sock/impl.h
index d2108f7..acc8c49 100644
--- a/sock/impl.h
+++ b/sock/impl.h
@@ -9,6 +9,6 @@ void sock_close(struct sock_data* sd);
 
 int sock_write(struct sock_data* sd, u8* data, size_t len, __be32 ip, __be16 port);
 
-void sock_read(struct sock_data* sd);
+int sock_read(struct sock_data* sd);
 
 #endif
diff --git a/types.h b/types.h
index d6703bb..f93bb12 100644
--- a/types.h
+++ b/types.h
@@ -8,6 +8,7 @@
 
 #include <crypt/types.h>
 #include <sock/types.h>
+#include <ips/types.h>
 
 struct dest_addr {
     __be32 ip;
@@ -19,16 +20,19 @@ struct tun_priv {
 
     struct sock_data* sd;
 
-    DECLARE_KFIFO_PTR(send_fifo, struct sk_buff *);
+    DECLARE_KFIFO_PTR(send_fifo, struct sk_buff*);
     spinlock_t send_lock;
     struct work_struct send_work;
 
     struct work_struct recv_work;
-    void (*orig_data_ready)(struct sock *sk);
+    void (*orig_data_ready)(struct sock* sk);
 
     struct dest_addr dest;
 
     struct crypt_data* cd;
+
+    struct ips_storage* ips;
+    spinlock_t ips_lock;
 };
 
 #endif
diff --git a/utils/impl.c b/utils/impl.c
index 56b3e97..7f6cb14 100644
--- a/utils/impl.c
+++ b/utils/impl.c
@@ -12,7 +12,7 @@ bool valid_ipv4_packet(unchar* buf, size_t len) {
 
 __be32 get_src_ip_from_ipv4_packet(unchar* buf, size_t len) {
     if (len < 20) {
-        return 0;
+        return -EPROTO;
     }
 
     __be32 val;
@@ -22,7 +22,7 @@ __be32 get_src_ip_from_ipv4_packet(unchar* buf, size_t len) {
 
 __be32 get_dst_ip_from_ipv4_packet(unchar* buf, size_t len) {
     if (len < 20) {
-        return 0;
+        return -EPROTO;
     }
 
     __be32 val;
